# 勤怠ハブ — お題選定から PoC 完了までの記録

日常の小さな面倒を解決するウェブアプリ/ツールを開発するにあたり、「お題の選定」と「技術検証(PoC)」を行った記録。2026-08-04 時点で PoC は **Go 判定**で完了。

---

## 1. お題決定の経緯

日常の定型作業の面倒を解消する個人開発として、複数の候補(朝活 Slack 投稿、美容院予約、休暇カレンダー入力、レバテック勤怠入力など)を「**頻度 × 1回あたりの苦痛**、かつ回避手段・既存代替がないこと」で比較し、**毎月末に必ず発生し回避不能な「レバテックの勤怠入力」**を本命に選んだ。

深掘りの過程で決定打となる事実が判明:

- 勤怠入力の苦痛の正体は「毎日ほぼ同じ値の延々手入力」+「カレンダーやメモからの転記」
- それとは別に、休暇・遅刻早退の**申請は別のフォームから都度行っており**、それもだるい(レバテック上で例外日を入力するわけではなく、独立した面倒)

2つは独立した面倒だが、どちらも**例外日(休暇・遅刻早退)の情報を起点にしている**——申請はその情報を提出する作業であり、勤怠入力ではその日をスキップ・調整する判断材料になり、カレンダー登録でも同じ情報を使う。ならば例外日を一箇所で管理し、そこから各所へ反映すればよい。ここから統合コンセプト「勤怠ハブ」が生まれた。

### 決定したお題: 勤怠ハブ

> 例外日(休暇・遅刻早退)と基本勤務時間を一箇所で管理し、月末にボタン一つでレバテックに自動入力する。

- **MVP スコープ**: レバテックへの勤怠時間の入力→保存のみ(請求書の提出は対象外)
- **拡張フェーズ**: ① Google カレンダーへの休暇予定反映、② 休暇申請プラットフォームへの自動申請
- スコープを絞る理由: 最大の不確実性(レバテック画面の自動操作)を最初に潰せば、残りは確実に作れる部分だけになる

---

## 2. PoC の設計

### 2-1. なぜ PoC が必要か

この題材の不確実性はコードを書く部分にはほぼなく、**「レバテック側が自動操作を許す構造かどうか」に全て集中**している。公開 API はないため、実現手段はブラウザ自動操作(Playwright)一択。作り込む前に、成立可否を最小コストで白黒つける必要があった。

### 2-2. 検証した仮説と、その理由

| # | 仮説 | なぜ検証するか |
|---|---|---|
| 1 | 保存したセッションで、後日**再ログインなし**に勤怠画面へ到達できる | ログインは Google SSO。Google は自動化ブラウザからのログインを拒否するため、「初回だけ手動ログイン→セッション使い回し」が成立しないと自動化の旨みが激減する。最大のリスク |
| 2 | 勤怠フォームは**機械的に入力・保存**できる | フォームが素直な HTML なら勝ち確だが、複雑な SPA や不安定なセレクタだと工数が跳ねる。難易度査定が必要 |
| 3 | 日々の「保存」と月次の「提出(報告)」が**分離**している | 勤怠は請求に直結する。誤って月次提出を踏む事故が構造上起きないことの確認(機能ではなく安全性の検証) |

### 2-3. 安全策: テスト値ではなく「正しい値」を入れる

仮説2の検証は本番データへの入力を伴う。そこで「どうせ入力する予定の正しい値を PoC で入れる」方針を採用した。

- 成功すればその月の入力が終わっているだけ(実利)
- 失敗しても手で直せる
- テスト用ダミー値の消し忘れ事故が構造上起きない

---

## 3. 検証の経過(ハマりどころと解決)

```
npm run login (Playwright起動のChromeで手動SSO)
  └─ ✗ Google が拒否「このブラウザまたはアプリは安全でない可能性があります」
     → 対策: ログインだけ自動化フラグなしの素の Chrome で同一プロファイルを開く

npm run login (素のChrome) → npm run check
  └─ ✗ ログイン画面に逆戻り。セッションが引き継がれない
     → 原因: Cookie 暗号鍵の不一致。素の Chrome は macOS Keychain の鍵で暗号化、
       Playwright は --use-mock-keychain(ダミー鍵)で起動するため復号できない
     → 対策: ログイン用の素の Chrome にも --use-mock-keychain を付けて鍵を揃える

npm run login (素のChrome + mock keychain) → npm run check
  └─ ✓ 再ログインなしでログイン済み画面に到達(仮説1クリア)

npm run fill (ドライラン)
  └─ ✓ 平日16日分に 9:00/18:00/1:00 が入力され、作業時間 8:00 が自動計算
       土日・祝日・休暇4日・入力済みの日はスキップ

npm run fill --save (1回目)
  └─ ✗ 入力は完走したが「保存する」のクリックがタイムアウト
     → 原因: 保存ボタンが <button> ではなく <a class="btnSaveReport"> だった
     → 対策: セレクタを a.btnSaveReport に変更

npm run fill --save (2回目)
  └─ ✓「更新しました。」表示、17.0日 / 136:30 が保存された(仮説2クリア)
```

仮説3は画面観察で確認: 保存後もステータスは「未報告」のままで、月次の「報告する」は別ボタン。**報告は意図的に自動化せず手動に残す**(誤提出防止の安全弁)。

---

## 4. 結果

### 判定: Go(技術的に検証可能 → 実装に進んでよい)

| 仮説 | 結果 |
|---|---|
| 1. セッション引き継ぎ | ✅ クリア(日をまたいでもセッション維持を確認) |
| 2. 自動入力→保存 | ✅ クリア |
| 3. 保存と提出の分離 | ✅ 確認済み |

### 副産物

- 「正しい値を入れる」方針により、**PoC 完了と同時に当月の勤怠入力が実際に終わった**
- 本実装にそのまま使える実セレクタ・構成(persistent context + 実 Chrome + mock keychain)・ハマりどころの知見

### 確定した技術構成

- Playwright + `launchPersistentContext` + `channel: 'chrome'`(実 Chrome)
- ログインのみ手動: 素の Chrome + `--use-mock-keychain` で同一プロファイルを開いて SSO
- 勤怠画面 `platform.levtech.jp/p/workreport/input/{id}/` は静的 HTML。行=日付、先頭3つの入力欄が開始/終了/休憩
- 保存は `a.btnSaveReport` をクリック。デフォルトはドライラン、`--save` 指定時のみ保存

---

## 5. 追加 PoC: 例外日申請フォーム(Typeform)の自動入力

拡張フェーズ②(休暇・遅刻早退の申請自動化)の不確実性も、同じレシピで先行検証した。

### 前提と設計

- 申請先はログイン不要の Typeform(`crmleverages.typeform.com`)。URL の `#id=ENGxxxx` で本人特定される
- 回答は最後の送信ボタンを押すまでサーバーに記録されないため、**全問回答して送信手前で停止するドライラン**が安全に成立
- 申請は提出後に取り下げ不可(営業担当者への連絡が必要)なので、送信までの本実行は「実際に申請したい例外日が発生したとき」に正しい内容で行う方針(レバテック PoC と同じ「どうせやる作業を PoC にする」方式)

### 検証結果: ドライラン成功

種別(お休み/遅参/早帰り等)の選択 → 分岐に応じた日付・理由の入力 → 任意項目のスキップ → 最終問の回答まで自動化し、「回答を送信する」の手前で停止することを確認。

**2026-08-10 追記: 実申請(`--submit`)成功で完全クローズ。** 8/10 の夏季休暇を実際に申請(種別=お休み・理由=その他・詳細=夏季休暇取得)。送信後の完了判定は `[data-qa="thank-you-screen"]` では検出できず、**body テキスト「ご回答ありがとうございました。」(部分一致「ありがとう」)で検出できた**。MVP の `submitted` 状態判定はこのテキスト検出を使う。

### ハマりどころ(Typeform 自動操作の知見)

```
✗ タブタイトルから現在の質問を特定 → タイトルが更新されないことがあり不安定
✗ :visible セレクタで要素特定 → Typeform は全質問を DOM に持ち、画面外の質問にも誤マッチ
✗ 物理クリック → オーバーレイがポインタイベントを横取りしてタイムアウト
✓ アクティブ質問は data-qa-block="true" かつ data-qa-focused="true" で特定し、全操作をその中に限定
✓ クリックは dispatchEvent('click')、テキストは fill + focus + Enter(ポインタ不使用)
✓ 送信判定もブロック内限定+「最終問に回答済みか」のチェックで誤検出を防止
```

---

## 6. 追加 PoC: Google カレンダーへの休暇予定登録(GAS)

拡張フェーズ①も同じレシピで検証し、2026-08-07 に **4仮説すべてクリアで Go 判定**。

### 前提と設計

これまでの2つと違い、カレンダーには公式 API があり登録した予定は消せる。不確実性の重心は「操作の可否」ではなく「ハブ(ローカル Node)からどう届けるか」の接続経路に移る。

- 構成: ローカル `05-calendar-sync.mjs` → HTTP POST → GAS Web App(`gas/Code.gs`)
- 認証: Web App は「自分として実行/全員アクセス可」でデプロイし、POST ボディの共有シークレット(スクリプトプロパティ ⇔ calendar.config.json)を照合。漏洩時の被害は「専用カレンダーに予定が増える」までに限定され、削除系関数は Web App に公開せずエディタ専用
- 登録先: 専用カレンダー「勤怠ハブ」を自動作成(メイン予定と混ざらない・一括削除が安全)
- 表現: 休暇=終日、遅参=9:00〜出社時刻、早帰り=退社時刻〜18:00 の時間帯予定
- 冪等性: イベントにタグ `kintaiHub=kind:date` を付け、既登録なら skip
- ドライラン優先を踏襲: GAS 側は `dryRun !== false` のときは登録せず計画一覧のみ返す(安全側デフォルト)。`--commit` 指定時のみ実登録

### 検証した仮説と結果

| # | 仮説 | 結果 |
|---|---|---|
| 1 | ローカルの Node から GAS 経由で予定を登録できる(外部起動+シークレット照合) | ✅ クリア |
| 2 | 終日・時間帯の予定が意図どおりの見た目(TZ=Asia/Tokyo、日付ズレなし) | ✅ クリア(目視確認) |
| 3 | 再実行しても重複登録されない | ✅ クリア(全件 `skipped(既登録)`) |
| 4 | ツール登録分(タグ付き)だけを識別して一括削除できる | ✅ クリア(手動予定は無傷) |

### 検証順の工夫

仮説4(削除)を本登録の**前**に置いた: サンプル値で仮説2・3を検証 → `manualDeleteAll` で掃除(これ自体が仮説4の検証)→ その後に実際の例外日で本登録。「ダミー値の消し忘れ事故が構造上起きない」流儀を、削除検証と掃除の一体化で実現。

### 知見(ハマる前に踏んだ地雷除け)

```
- GAS のタイムゾーンは appsscript.json で Asia/Tokyo を明示(ズレると予定日が1日ずれる)。
  レスポンスに timeZone を含め、ローカル側で検証・警告する
- Web App の「アクセスできるユーザー」は「全員」(「Google アカウントを持つ全員」だと
  POST にログイン画面の HTML が返る)。ローカル側は JSON 以外の応答を検出して案内を出す
- GAS への POST は 302 リダイレクトされるため redirect: 'follow' 必須
- Content-Type は text/plain(GAS の定番。将来ブラウザから叩く際も CORS preflight を踏まない)
- コード修正後は「デプロイを管理 → 編集 → 新バージョン」で再デプロイしないと反映されない
```

### 完成形アーキテクチャの合意

ハブの休暇登録インターフェースからアダプタパターンで各所へ反映する:

- カレンダーアダプタ: 純粋な HTTP POST(今回検証した GAS Web App)
- Typeform アダプタ: Playwright によるフォーム自動入力(Typeform に回答作成の公式 API は存在せず、Responses API は読み取り専用。フォームはレバレジーズ所有のためトークンも取得不可)
- 帰結: Typeform アダプタが Chrome を要するため、ハブはローカル実行が前提。カレンダーのみなら外出先からでも叩ける分離構造

---

## 7. 今後

1. **MVP 化**: MVP_SPEC.md(v2)に基づき実装。例外日ストアの一元化・3アダプタ統合・ローカル Web UI
2. ~~申請フォームの本番送信~~ → 2026-08-10 完了(§5 追記参照)
3. ~~対象月 URL の自動解決~~ → 2026-08-10 スパイク完了(06-resolve-url-spike.mjs)。一覧 `/p/work-report/` の各行が期間テキストと `/p/workreport/{id}/` への実 href を持ち、決定的に解決可能。詳細ページの「報告月」表示を月照合ガードに使う(確定した方式は MVP_SPEC.md §10)

**これで全ての技術的不確実性が解消。** 残るは確実に作れる実装のみ。
